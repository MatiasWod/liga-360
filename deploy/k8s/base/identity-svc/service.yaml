apiVersion: v1
kind: Service
metadata:
  name: identity-svc
  labels:
    app: identity-svc
    app.kubernetes.io/part-of: liga360
spec:
  type: ClusterIP
  ports:
    - port: 4005
      targetPort: 4005
  selector:
    app: identity-svc
