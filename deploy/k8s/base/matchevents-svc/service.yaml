apiVersion: v1
kind: Service
metadata:
  name: matchevents-svc
  labels:
    app: matchevents-svc
    app.kubernetes.io/part-of: liga360
spec:
  type: ClusterIP
  ports:
    - port: 4006
      targetPort: 4006
  selector:
    app: matchevents-svc
