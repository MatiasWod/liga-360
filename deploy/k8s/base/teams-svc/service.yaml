apiVersion: v1
kind: Service
metadata:
  name: teams-svc
  labels:
    app: teams-svc
    app.kubernetes.io/part-of: liga360
spec:
  type: ClusterIP
  ports:
    - port: 4002
      targetPort: 4002
  selector:
    app: teams-svc
