apiVersion: v1
kind: Service
metadata:
  name: inscriptions-svc
  labels:
    app: inscriptions-svc
    app.kubernetes.io/part-of: liga360
spec:
  type: ClusterIP
  ports:
    - port: 4004
      targetPort: 4004
  selector:
    app: inscriptions-svc
