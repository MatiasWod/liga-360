apiVersion: v1
kind: Service
metadata:
  name: tournaments-svc
  labels:
    app: tournaments-svc
    app.kubernetes.io/part-of: liga360
spec:
  type: ClusterIP
  ports:
    - port: 4001
      targetPort: 4001
  selector:
    app: tournaments-svc
