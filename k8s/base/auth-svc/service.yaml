apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  labels:
    app: auth-svc
    app.kubernetes.io/part-of: liga360
spec:
  type: ClusterIP
  ports:
    - port: 4003
      targetPort: 4003
  selector:
    app: auth-svc
